<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>247428aa-2f64-422f-b919-070f7623acbf</groupId>
    <artifactId>deployment-to-cloudhub-2.0</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>mule-application</packaging>

    <name>deployment-to-cloudhub-2.0</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <muleVersion>4.9.0</muleVersion>
        <runtime.version>4.9.0</runtime.version>

        <environment>Sandbox</environment>
        <region>us-east-2</region>
        <appName>deployment-to-cloudhub-2.0-dev</appName>

        <anypoint.username>${mule.anypoint.username}</anypoint.username>
        <anypoint.password>${mule.anypoint.password}</anypoint.password>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.2.0</version>
            </plugin>

            <plugin>
                <groupId>org.mule.tools.maven</groupId>
                <artifactId>mule-maven-plugin</artifactId>
                <version>4.6.1</version>
                <extensions>true</extensions>

                <configuration>
                    <cloudHub2Deployment>
                        <uri>https://anypoint.mulesoft.com</uri>

                        <organizationId>247428aa-2f64-422f-b919-070f7623acbf</organizationId>
                        <environmentId>8f9731fb8ff042e48bd1b9e309e73620</environmentId>

                        <runtimeVersion>${runtime.version}</runtimeVersion>

                        <authentication>
                            <username>${anypoint.username}</username>
                            <password>${anypoint.password}</password>
                        </authentication>

                        <deployment>
                            <applicationName>${appName}</applicationName>
                            <replicas>1</replicas>
                        </deployment>
                    </cloudHub2Deployment>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-http-connector</artifactId>
            <version>1.10.3</version>
            <classifier>mule-plugin</classifier>
        </dependency>

        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-sockets-connector</artifactId>
            <version>1.2.5</version>
            <classifier>mule-plugin</classifier>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>anypoint-exchange-v3</id>
            <name>Anypoint Exchange</name>
            <url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
            <layout>default</layout>
        </repository>
        <repository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <layout>default</layout>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repo</name>
            <layout>default</layout>
            <url>https://repository.mulesoft.org/releases/</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>
